fn factorial(n: Int) -> Int {
    if n <= 1 {
        return 1;
    }
    return n * factorial(n - 1);
}

fn greet(name: String) -> Unit {
    print("Hello, " + name);
}

greet("Karma");
print(factorial(10));
